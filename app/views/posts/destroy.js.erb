$(document).ready(function() {
  $(document).on('ajax:success', function() {
    $('#post_<%= @post.id %>').fadeOut(500);
  });
  $('#post_<%= @post.id %>').css("background-color", "#FFBBBB").delay( 7000 ); 
});
